<div id="adm">
    <div class="form">
        <%= form_with url: "/autAdm" , method: :post do |form| %>
            <div class="form-field">
                <%= form.label :password, "senha: " %>
                <%= form.password_field :password %>
            </div>
            <%= form.submit "entrar" %>
            <% end %>
    </div>
</div>